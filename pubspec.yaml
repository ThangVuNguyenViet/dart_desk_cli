name: dart_desk_cli
description: CLI tool for deploying Dart Desk studios to the cloud.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

executables:
  dart_desk: dart_desk

dependencies:
  args: ^2.5.0
  http: ^1.2.0
  archive: ^4.0.2
  yaml: ^3.1.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
